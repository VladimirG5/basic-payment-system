services:
  mysql:
    image: mysql:8.0
    container_name: banking-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: banking
      MYSQL_USER: banking
      MYSQL_PASSWORD: bankingpass
    ports:
      - "3307:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-prootpassword"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  payment-core-service:
    build:
      context: ./payment-core-service
    container_name: payment-core-service
    environment:
      DB_HOST: mysql
      DB_PORT: 3306
      DB_NAME: banking
      DB_USERNAME: banking
      DB_PASSWORD: bankingpass
    ports:
      - "8081:8081"
    depends_on:
      mysql:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8081/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  auth-service:
    build:
      context: ./auth-service
    container_name: auth-service
    environment:
      # Dev-only signing key committed for local/demo compose - not a real secret. Must match
      # gateway-service's JWT_SECRET below - auth-service issues tokens, gateway validates them.
      JWT_SECRET: dev-only-signing-key-change-me-before-any-real-deploy-32b
      PAYMENT_CORE_BASE_URL: http://payment-core-service:8081
    ports:
      - "8082:8082"
    depends_on:
      payment-core-service:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8082/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  gateway-service:
    build:
      context: ./gateway-service
    container_name: gateway-service
    environment:
      DB_HOST: mysql
      DB_PORT: 3306
      DB_NAME: banking
      DB_USERNAME: banking
      DB_PASSWORD: bankingpass
      # Dev-only signing key committed for local/demo compose - not a real secret.
      JWT_SECRET: dev-only-signing-key-change-me-before-any-real-deploy-32b
      PAYMENT_CORE_BASE_URL: http://payment-core-service:8081
    ports:
      - "8080:8080"
    depends_on:
      payment-core-service:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  frontend:
    build:
      context: ./frontend
    container_name: frontend
    ports:
      - "3000:80"
    depends_on:
      gateway-service:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1/"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 15s

volumes:
  mysql-data:
